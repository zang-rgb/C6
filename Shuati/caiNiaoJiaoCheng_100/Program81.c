/*
题目：809*??=800*??+9*?? 其中??代表的两位数, 809*??为四位数，8*??的结果为两位数，9*??的结果为3位数。求??代表的两位数，及809*??后的结果。
*/

#include <stdio.h>

int main(void)
{
    int a;
    for (int i = 10; i < 100; i++)
        if ((809 * i == 800 * i + 9 * i) && (809 * i < 10000) && (8 * i < 100) && (9 * i > 99))
            printf("%d\t%d\t", i, 809 * i);
    return 0;
}