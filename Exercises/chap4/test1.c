#include <stdio.h>
int main(void)
{
  /*
  int age;
  int num;

  scanf("%d%d", &age, &num);
  printf("age = %d, num = %d", age, num);
  */
  char name[40];
  scanf("%s", name);
  printf("name = %s", name);

  return 0;
}